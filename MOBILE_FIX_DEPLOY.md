# 🚀 Инструкция по деплою фиксов мобильной версии

## Что было исправлено

### 1. ✅ Скролл на мобильных (`globals.css`)
**Было:**
```css
body {
  overflow: hidden; /* Полностью блокировал скролл */
}
```

**Стало:**
```css
body {
  overflow-x: hidden; /* Только горизонтальный скролл скрыт */
}
```

### 2. ✅ Touch-события на canvas (`globals.css`)
**Было:**
```css
canvas {
  touch-action: none; /* Блокировал все тач-события */
}
```

**Стало:**
```css
canvas {
  touch-action: pan-y pinch-zoom; /* Разрешает скролл и зум */
}
```

### 3. ✅ Производительность на мобильных (`SequenceScroll.tsx`)
**Проблема:** 384 изображения (192 + 192) вызывали краш на мобильных

**Решение:** 
- На мобильных устройствах canvas полностью отключается
- Вместо него показывается статичный градиентный фон
- Экономит память и позволяет сайту работать

### 4. ✅ Viewport meta-тег (`layout.tsx`)
Добавлены настройки viewport для корректного отображения на мобильных.

---

## 📦 Как задеплоить

### Вариант 1: Автоматический деплой (рекомендуется)

1. **Запушьте изменения на GitHub:**
   ```bash
   cd lutovichsand
   git push origin main
   ```

2. **Vercel автоматически задеплоит** новую версию

3. **Проверьте сайт** на мобильном через несколько минут

### Вариант 2: Ручной деплой через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Залогиньтесь
vercel login

# Деплой
vercel --prod
```

---

## 🧪 Как проверить

Откройте сайт на мобильном устройстве или в Chrome DevTools:

1. **F12 → Toggle Device Toolbar (Ctrl+Shift+M)**
2. Выберите iPhone или Android
3. Перезагрузите страницу (Ctrl+R)
4. Проверьте:
   - ✅ Скролл работает
   - ✅ Контент отображается
   - ✅ Нет ошибок в консоли

---

## 🔧 Если проблемы остались

### Проверьте консоль ошибок:
```bash
# Локально
npm run dev
# Откройте http://localhost:3000 и проверьте консоль
```

### Возможные дополнительные фиксы:
- Если на очень старых устройствах всё ещё не работает — увеличьте breakpoint для определения мобильных в `SequenceScroll.tsx` (сейчас 768px)
- Добавьте ленивую загрузку изображений через `loading="lazy"`

---

**Статус:** ✅ Код готов, осталось запушить и задеплоить
